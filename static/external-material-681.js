/* Direct creation flow for safe external teaching media. */
(function(){'use strict';
  async function admin(){const key=await window.getAdminKey();if(!key)throw new Error('需要管理者驗證');return key;}
  async function json(url,options={}){const key=await admin(),r=await fetch(url,{...options,headers:{'X-Admin-Key':key,'Content-Type':'application/json',...(options.headers||{})}}),d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.error||'操作失敗');return d;}
  function setOptions(id,items,empty){const el=document.getElementById(id);if(!el)return;el.innerHTML=`<option value="">${empty}</option>`+(items||[]).map(x=>`<option value="${String(x.id).replace(/"/g,'&quot;')}">${window.escapeHtml?window.escapeHtml(x.title||x.id):x.title||x.id}</option>`).join('');}
  window.openExternalMaterialDrawer=async function(){const drawer=document.getElementById('external-material-drawer');if(!drawer)return;drawer.classList.remove('hidden');try{const area=document.getElementById('admin-material-area')?.value||'internal',group=document.getElementById('admin-material-group')?.value||window.currentGroupKey||'grpBio';document.getElementById('external-material-area').value=area;const groupSelect=document.getElementById('external-material-group');groupSelect.innerHTML=[...document.querySelectorAll('#admin-material-group option')].map(o=>`<option value="${o.value}" ${o.value===group?'selected':''}>${o.textContent}</option>`).join('');const [courses,categories]=await Promise.all([json(`/api/courses/admin?area=${encodeURIComponent(area)}&group=${encodeURIComponent(group)}`),json(`/api/quiz-categories/admin?area=${encodeURIComponent(area)}&group=${encodeURIComponent(group)}`)]);setOptions('external-material-course',courses,'未指定課程');setOptions('external-material-category',categories,'未指定考卷');}catch(e){document.getElementById('external-material-preview').textContent='❌ '+e.message;}};
  window.saveExternalMaterialLink=async function(){const preview=document.getElementById('external-material-preview');try{const body={title:document.getElementById('external-material-title').value.trim(),description:document.getElementById('external-material-description').value.trim(),area:document.getElementById('external-material-area').value,group:document.getElementById('external-material-group').value,courseId:document.getElementById('external-material-course').value,category:document.getElementById('external-material-category').value,url:document.getElementById('external-material-url').value.trim()};preview.textContent='⏳ 驗證 provider 並建立教材…';const created=await json('/api/materials/external',{method:'POST',body:JSON.stringify(body)});preview.textContent=`✅ 已建立 ${created.material?.title||'外部教材'}（${created.externalMedia?.provider||'external'}）；未使用 Worker 或物件儲存。`;if(typeof window.invalidateAdminMaterialsCache==='function')window.invalidateAdminMaterialsCache();return created;}catch(e){preview.textContent='❌ '+e.message;return null;}};
  window.closeExternalMaterialDrawer=function(){document.getElementById('external-material-drawer')?.classList.add('hidden');};

  // Stable aliases keep the direct-create owner reachable even when a later
  // compatibility module reuses the historical global names. The Teacher
  // content composer calls these aliases instead of depending on script order.
  window.openExternalMaterialCreateDrawer=window.openExternalMaterialDrawer;
  window.createExternalMaterialFromDrawer=window.saveExternalMaterialLink;
})();
